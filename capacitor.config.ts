import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seriesapiens.app',
  appName: 'SerieSapiens',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
